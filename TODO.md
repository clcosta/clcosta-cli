1. [x] Add the b64 command (Encrypt/Decrypt)
2. [ ] Fix the error on set enviroment and copy template folder in the first run
3. [ ] Check the load config yaml file
4. [ ] Make the all commands use config to handle with dynamic operations
5. [ ] Add the secrets command
 - The secrets command behavior: Encrypt files with some passphrase and only with the CLI the file can be read
 - The secrets will be used like "interface" for acess the file. Very useful for password files
